{
  "name": "cr-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CR CLI monorepo (TypeScript, Bun runtime).",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:web-bundle": "bun run scripts/build-web-bundle.ts",
    "dev": "bun run packages/cli/src/cli.ts",
    "help": "bun run packages/cli/src/cli.ts help",
    "init": "bun run packages/cli/src/cli.ts init",
    "review": "bun run packages/cli/src/cli.ts review",
    "create-review": "bun run packages/cli/src/cli.ts create-review",
    "create-mr": "bun run packages/cli/src/cli.ts create-mr",
    "serve": "bun run build:web-bundle && bun run packages/cli/src/cli.ts serve",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint packages/ tests/",
    "lint:fix": "biome lint --write packages/ tests/",
    "format": "biome format --write packages/ tests/",
    "format:check": "biome format packages/ tests/",
    "build:bin": "bun run build:web-bundle && bun build --compile --minify packages/cli/src/cli.ts --outfile dist/cr",
    "build:bin-compress": "bun run build:web-bundle && bun build --compile --minify packages/cli/src/cli.ts --outfile dist/cr && (upx dist/cr.exe || echo 'upx not found, skipping compression')",
    "build": "bun run typecheck && bun run build:bin"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.7",
    "@types/bun": "latest",
    "@types/node": "^24.6.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3",
    "vite": "^8.0.0"
  }
}
