{
  "name": "@pv/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/bun/index.ts"
  },
  "scripts": {
    "start": "electrobun run",
    "icons": "bun ../../scripts/generate-desktop-icons.ts",
    "build:dev": "bun install && bun run icons && electrobun build",
    "build:canary": "bun install && bun run icons && electrobun build --env=canary",
    "build:stable": "bun install && bun run icons && electrobun build --env=stable",
    "dev": "bun run build:dev && electrobun dev",
    "dev:watch": "bun run icons && electrobun dev --watch"
  },
  "dependencies": {
    "@pv/core": "workspace:*",
    "@pv/server": "workspace:*",
    "electrobun": "^1.16.0"
  }
}
